# ScratchJr micro:bit firmware

Descarga un `.hex` con nombre Bluetooth personalizado.

Sitio: https://<usuario>.github.io/<repo>/

## Actualizar firmware base

Reemplazar `docs/firmware/scratchjr-microbit-base.hex` con un build reciente
(slot de 20 bytes `ScratchJr` + espacios para parcheo).
